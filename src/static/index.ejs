<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>激活页面</title>
</head>

<body>
    <div class="app">
        <h2>您的账号已成功激活，请前往登录：<a href="javascript:;" onclick="goto()">立即跳转</a></h2>
        <br/>
        <h2><span id="show"></span>   秒后自动跳转到登录页"</h2>
    </div>
    <script>
        function goto() {
            location.href = "//localhost:8080/user/login";
        }
        // 倒计时
        var t = 10;
        setInterval("refer()", 1000);

        function refer() {
            if (t < 0) {
                location.href = "//localhost:8080/user/login";
            } else {
                document.getElementById('show').innerHTML = "" + t ;
            }
            t--;
        }
    </script>
</body>
<style>
    .app {
        width: 500px;
        height: 500px;
        margin: auto;
        margin-top: 30vh;
    }
    #show {
        color: rgb(255, 60, 0);
    }
</style>

</html>